<section class="hero">
  <div class="character-container"></div>
  <div class="hero-content">
    <div class="hero-left">
      <h1 class="hero-title">
        THE<br />
        DUNGEON<br />
        CRAWLER<br />
        CARL<br />
        SAGA
      </h1>
    </div>
    <div class="hero-right">
      <div class="books-container">
        <img src="/src/assets/book.jpg" class="book" alt="Book Cover" />
        <img src="/src/assets/book2.jpg" class="book" alt="Book Cover" />
        <img src="/src/assets/book3.jpg" class="book" alt="Book Cover" />
        <img src="/src/assets/book4.jpg" class="book" alt="Book Cover" />
        <img src="/src/assets/book5.jpg" class="book" alt="Book Cover" />
        <img src="/src/assets/book6.jpg" class="book" alt="Book Cover" />
        <img src="/src/assets/book7.jpg" class="book" alt="Book Cover" />
        <img src="/src/assets/donut.png" class="book" alt="Book Cover" />
      </div>
    </div>
  </div>
</section>
<style>
  body {
    background-color: rgb(16, 16, 16);
    height: 200vh;
  }

  body::before {
    content: "";
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    position: fixed;
    /* Set the size of each grid square */
    background-size: 40px 40px;

    /* Draw vertical and horizontal lines */
    background-image: radial-gradient(circle, #e0bb263e 1px, transparent 1px);
    z-index: -1;
  }

  section {
    min-height: 100vh;
    width: 100%;
  }

  .hero-content {
    /* position: relative; */
    width: 100%;
    height: 100vh;
    /* margin: 0 auto; */
    /* z-index: 1; */
  }

  .books-container {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .hero-content::before {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 300px;
    background-image: url("src/assets/wave2.svg");
    background-repeat: no-repeat;
    background-position: bottom;
    background-size: cover;
    filter: drop-shadow(0 2px 10px rgb(1, 1, 1));
    pointer-events: none;
    z-index: 10;
  }

  .hero-content::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 300px;
    background-image: url("src/assets/wave-top2.svg");
    background-repeat: no-repeat;
    background-position: top;
    background-size: cover;
    filter: drop-shadow(0 2px 10px rgb(1, 1, 1));
    pointer-events: none;
    z-index: 10;
  }

  .book {
    position: absolute;
    width: 200px;
    animation: float 6s ease-in-out infinite;
  }

  .book:nth-child(1) {
    top: 8%;
    left: 25%;
    --rotation: -8deg;
    animation-delay: 0.2s;
    z-index: 1;
  }

  .book:nth-child(2) {
    top: 25%;
    left: 10%;
    --rotation: -15deg;
    animation-delay: 1s;
    z-index: 2;
  }

  .book:nth-child(3) {
    top: 25%;
    left: 40%;
    --rotation: 8deg;
    animation-delay: 0.6s;
    z-index: 2;
  }

  .book:nth-child(4) {
    top: 45%;
    left: 20%;
    --rotation: -6deg;
    animation-delay: 0.3s;

    z-index: 3;
  }

  .book:nth-child(5) {
    top: 48%;
    left: 45%;
    --rotation: 10deg;
    animation-delay: 0.8s;

    z-index: 3;
  }

  .book:nth-child(6) {
    top: 55%;
    left: 70%;
    --rotation: 10deg;
    animation-delay: 1s;

    z-index: 4;
  }
  .book:nth-child(7) {
    top: 55%;
    left: 0;
    --rotation: -10deg;
    animation-delay: 0.4s;

    z-index: 4;
  }

  .book:last-child {
    bottom: 0;
    left: 20%;
    width: 350px;
    /* transform: translateX(-50%); */
    z-index: 9;
    filter: drop-shadow(-20px 1px 20px rgb(1, 1, 1));
    --rotation: 0deg;
  }

  .hero {
    min-height: 100vh;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 110%;
    height: 100%;
    background-image: url("src/assets/rays.webp");
    background-size: contain;
    /* background-position: ; */
    background-repeat: no-repeat;
    filter: brightness(0.5);
    z-index: -1;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;

    height: 100vh;
    width: min(1400px, 100%);
    margin: 0 auto;
    padding-inline: 4rem;
    gap: 3rem;
  }

  .hero-left {
    display: flex;
    align-items: center;
  }

  .hero-right {
    display: grid;
  }

  .hero-title {
    font-family: "Bebas Neue", sans-serif;
    font-size: clamp(3.5rem, 9vw, 7rem);
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0px;
    color: #f2c14d;
    letter-spacing: 3px;
    line-height: 0.85;

    text-shadow:
      4px 4px 0 #7c1c1c,
      8px 8px 0 rgba(0, 0, 0, 0.4);

    transform: skew(-2deg);
    filter: contrast(1.15) saturate(1.2);
  }

  @keyframes float {
    0%,
    100% {
      transform: rotate(var(--rotation)) translateY(0px);
    }

    50% {
      transform: rotate(var(--rotation)) translateY(-10px);
    }
  }
</style>
